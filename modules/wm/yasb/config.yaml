watch_config: true
bars:
  main_bar:
    enabled: true
    screens: ["\\\\.\\DISPLAY1"]
    class_name: "yasb-bar"
    alignment:
      position: "top"
      align: "center"
    dimensions:
      width: "100%"
      height: 30
    window_flags:
      windows_app_bar: true
      always_on_top: false
    widgets:
      left: ["komorebi_workspaces", "taskbar"]
      center: ["clock"]
      right: ["cpu", "memory", "battery", "lock_button", "power_button"]
  sub_bar:
    enabled: true
    screens: ["ZS-156"]
    class_name: "yasb-bar"
    alignment:
      position: "top"
      align: "center"
    dimensions:
      width: "100%"
      height: 30
    window_flags:
      windows_app_bar: true
      always_on_top: false
    widgets:
      left: ["komorebi_workspaces", "taskbar"]
      center: ["clock"]
      right: ["cpu", "memory", "battery", "lock_button", "power_button"]

widgets:
  komorebi_workspaces:
    type: "komorebi.workspaces.WorkspaceWidget"
    options:
      label_offline: ""
      label_workspace_btn: "{index}"
      label_workspace_active_btn: "{index}"
      label_workspace_populated_btn: "{index}"
      label_default_name: "{index}"
      label_zero_index: false
      hide_empty_workspaces: true
      hide_if_offline: true
  clock:
    type: "yasb.clock.ClockWidget"
    options:
      label: "{%a %d %b | %H:%M:%S}"
  cpu:
    type: "yasb.cpu.CpuWidget"
    options:
      label: "CPU: {info[percent][total]:.0f}%"
      update_interval: 2000
  memory:
    type: "yasb.memory.MemoryWidget"
    options:
      label: "RAM: {virtual_mem_percent}%"
      update_interval: 5000
  battery:
    type: "yasb.battery.BatteryWidget"
    options:
      label: "BAT: {percent}%"
      update_interval: 5000
  taskbar:
    type: "yasb.taskbar.TaskbarWidget"
    options:
      show_only_visible: true
      strict_filtering: true
      icon_size: 14
      title_label:
        enabled: true
        show: "always"
        max_length: 15
  lock_button:
    type: "yasb.custom.CustomWidget"
    options:
      class_name: "lock-button"
      label: "\uf023"
      callbacks:
        on_left: "exec rundll32.exe user32.dll,LockWorkStation"
  power_button:
    type: "yasb.custom.CustomWidget"
    options:
      class_name: "power-button"
      label: "\uf011"
      callbacks:
        on_left: "exec shutdown /r /t 0"
