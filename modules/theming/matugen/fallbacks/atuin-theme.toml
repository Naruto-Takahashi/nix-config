# atuin テーマ フォールバック (このファイルを更新した時点の実際の壁紙色を焼き込み)
# (matugen 未適用の環境向け。matugen-apply が同パスを上書きする。
#  配色キーの対応は templates/atuin-theme.toml と揃えること)
[theme]
name = "matugen"

[colors]
Base = "#e1e2e8"
# Title = 検索ボックス枠のタイトル ("GLOBAL" 等のフィルタ名) と枠線の色。
# フィルタモードに関わらず常にこのメインハイライト (accent) で固定する
Title = "#a2c9fd"
Important = "#a2c9fd"
Annotation = "#d7bde4"
# Guidance = 経過時間 (◯m ago) の列 (tertiary)
Guidance = "#d7bde4"
Muted = "#c3c6cf"
# AlertInfo/AlertError = 実行時間列の成功/失敗色。zsh の
# syntax-highlighting (存在する/しないコマンド) と同じ固定値
AlertInfo = "#98bb6c"
AlertError = "#e46876"
# AlertWarn = 選択行内の検索一致文字。フォールバック secondary と同色
AlertWarn = "#bbc7db"
