# 📁 Yazi — CUIファイルマネージャ 設定・使い方詳細

Yaziは**Rust製の高速CUIファイルマネージャ**です．  
非同期I/Oと非同期プレビューによる極めて高いレスポンス性が特長で，Vim風のキーバインドを採用しています．  
本環境では **kanagawa-dragon フレーバー** をベースに，Matugen パレット (壁紙連動) でフォルダ色・ファイル種別色・ステータスバーを動的に上書きしています ([matugen-palette.md](matugen-palette.md) 参照)．

---

## 🚀 起動方法

```bash
yazi          # 通常起動（カレントディレクトリ）
yazi <path>   # 指定ディレクトリで起動
```

> [!TIP]
> **シェル統合（`ya` ラッパー）**: `enableZshIntegration = true` により，Yazi終了時に**シェルのカレントディレクトリが自動的に移動先に変わります**．  
> `cd` の代わりに `yazi` を使うことで，Yaziで移動した場所からそのまま作業を続けられます．

---

## 🗺️ 基本的な画面構成

```
┌─────────────────────────────────────────────────────┐
│  [親ディレクトリ]  │  [現在のディレクトリ]  │  [プレビュー]  │
│  (左ペイン)        │  (中央ペイン・メイン)   │  (右ペイン)    │
└──────────────────────────────────────────────────────┘
│  [モード表示]  [パーミッション]  [ファイル位置/総数]  [時刻]  │
└─────────────────────────────────────────────────────┘
```

- **左ペイン**: 親ディレクトリの内容（移動の手がかり）
- **中央ペイン**: 現在のディレクトリ（主な操作対象）
- **右ペイン**: 選択中ファイルのプレビュー（テキスト/画像/動画等に対応）
- **Markdown/CSV/JSON/Jupyter Notebook/reStructuredText**: `rich-preview`
  (yaziPlugins.rich-preview、`rich-cli`を利用) でレンダリングして表示する。
  生テキストではなく装飾された状態で見える。yaziの実mime判定は`.md`を
  `text/plain`として返すため、拡張子(`url`)で直接マッチさせている
  (`modules/apps/yazi/default.nix`)
  - word/excel等のOffice文書は非対応 (`office.yazi`はLibreOffice依存で
    重いため見送り済み)
  - PDF/画像プレビューが小さく表示される既知の制約あり。WezTerm+WSLg環境で
    ピクセルサイズ問い合わせ (`CSI 14t`) が正しく応答されず、yaziが
    安全側の小さいサイズで描画するため。原因はWezTerm/yazi/WSLgの
    組み合わせによるものと推定、未解決

---

## ⌨️ キーバインド一覧

### 移動・ナビゲーション

| キー | 動作 |
|------|------|
| `j` / `↓` | 1つ下に移動 |
| `k` / `↑` | 1つ上に移動 |
| `h` / `←` | 親ディレクトリへ移動 |
| `l` / `→` / `Enter` | ディレクトリに入る / ファイルを開く |
| `gg` | リスト先頭へジャンプ |
| `G` | リスト末尾へジャンプ |
| `<C-u>` | 半ページ上スクロール |
| `<C-d>` | 半ページ下スクロール |
| `~` / `H` | ホームディレクトリへ移動 |
| `%` | カレントワーキングディレクトリへ移動（CWD） |
| `[` | 前のタブへ |
| `]` | 次のタブへ |

### ファイル操作

| キー | 動作 |
|------|------|
| `o` / `Enter` | ファイルを開く（規定アプリ） |
| `O` | ファイルを開くアプリを選択 |
| `y` | ファイルをコピー（ヤンク） |
| `x` | ファイルをカット |
| `p` | ファイルをペースト |
| `P` | 強制上書きペースト |
| `d` | ゴミ箱へ送る |
| `D` | 完全削除（確認あり） |
| `a` | 新規ファイル作成（末尾に `/` でディレクトリ作成） |
| `r` | ファイル名を変更（リネーム） |
| `;` | シェルコマンドを実行 |
| `!` | インタラクティブシェルを起動 |

> [!NOTE]
> 本環境では `o` キー（開く）に WSL対応の `wsl-open` を使用しています．  
> WSL環境では `wsl-open` が利用でき，Windows側の規定アプリでファイルを開けます．  
> デスクトップ環境では `xdg-open` にフォールバックします．

### 選択・マーキング

| キー | 動作 |
|------|------|
| `Space` | カーソル行をトグル選択 |
| `v` | ビジュアル選択モード（範囲選択） |
| `V` | 逆選択（選択反転） |
| `<Esc>` | 選択を解除 |
| `u` | 操作をUndo |

### 検索・フィルター

| キー | 動作 |
|------|------|
| `/` | ファイル名で検索（前方） |
| `?` | ファイル名で検索（後方） |
| `n` | 次の検索結果へ |
| `N` | 前の検索結果へ |
| `f` | フィルター（絞り込み表示） |
| `F` | グローバル検索（fd連携） |

### ソート・表示切替

| キー | 動作 |
|------|------|
| `,m` | 更新日時でソート |
| `,M` | 更新日時でソート（逆順） |
| `,s` | ファイルサイズでソート |
| `,S` | ファイルサイズでソート（逆順） |
| `,e` | 拡張子でソート |
| `,r` | ランダムソート |
| `.` | 隠しファイルの表示・非表示トグル |

### タブ管理

| キー | 動作 |
|------|------|
| `t` | 新しいタブを開く |
| `1`〜`9` | 指定番号のタブに切り替え |
| `[` | 前のタブへ移動 |
| `]` | 次のタブへ移動 |

### コピー・クリップボード連携

| キー | 動作 |
|------|------|
| `yy` | ファイルをコピー（ヤンク） |
| `yn` | ファイル名をシステムクリップボードにコピー |
| `yp` | ファイルパス（絶対）をクリップボードにコピー |
| `yd` | ディレクトリパスをクリップボードにコピー |

### アーカイブ操作 (compress.yazi / ouch.yazi)

| キー | 動作 |
|------|------|
| `c` `a` `a` | 選択中のファイル/フォルダをアーカイブ化 (既定zip) |
| `c` `a` `p` | アーカイブ化 (パスワード保護) |
| `c` `a` `l` | アーカイブ化 (圧縮率を指定) |
| `Enter` (アーカイブファイル上) | `ouch`でその場に展開 |

### ディレクトリジャンプ (zoxide)

| キー | 動作 |
|------|------|
| `c` `d` | zoxideの利用履歴からディレクトリ名で検索してジャンプ (内部でfzf使用) |

yazi組み込みのzoxideプラグイン (既定は`Z`キーのみだが`c,d`にも割り当て)。
zshの`cd`と同じzoxide履歴を共有するので、シェルでよく行く場所はyazi側でも
同じ候補で出てくる。配色は`_ZO_FZF_OPTS`ではなく`YAZI_ZOXIDE_OPTS`環境変数で
決まる (`modules/shell/zsh/functions.zsh`)。

### その他

| キー | 動作 |
|------|------|
| `q` | Yaziを終了 |
| `Q` | Yaziを終了（シェルのCDを更新しない） |
| `?` | キーバインド一覧（ヘルプ）を表示 |
| `~` | テーマプレビューを表示 |
| `W` | 操作ログパネルを表示 |
| `w` | タスクマネージャー（進行中のタスク）を表示 |

### プレビュー操作

| キー | 動作 |
|------|------|
| `J` (Shift+j) | プレビューを 5 行下にスクロール |
| `K` (Shift+k) | プレビューを 5 行上にスクロール |
| `<C-e>` | プレビューを 1 行下にスクロール |
| `<C-y>` | プレビューを 1 行上にスクロール |

---

## 🎨 テーマカスタマイズ（本環境の設定）

設定ファイル: [`modules/yazi.nix`](../modules/yazi.nix)

### 色彩設定（Matugen パレット準拠）

現在の配色は壁紙から動的生成されます: フォルダ = secondary，ドキュメント系 = tertiary，スクリプト/メディア系 = complement，Web/データ系 = triad，コンパイル言語/アーカイブ = error．ステータスバーは Starship / lualine と同じ powerline デザインです．以下は移行前の静的配色の記録です．

| 要素 | 色コード | 説明 |
|------|---------|------|
| ディレクトリアイコン・テキスト | `#5ea1ff` | cyberdream `blue`（nvimの `Directory` ハイライトと統一） |
| 選択行背景（current） | `#3c4048` | cyberdream `bg_highlight`（nvimの `CursorLine` と統一） |
| 親ディレクトリ行背景（parent） | `#3c4048` | 同上 |
| CWD表示 | `#5ef1ff` | cyberdream `cyan` |
| パーミッション区切り | `#5ea1ff` | cyberdream `blue` |
| 実行権 | `#5eff6c` | cyberdream `green` |
| 書き込み権 | `#ff6e5e` | cyberdream `red` |

### 透過設定

```toml
[app]
overall = { bg = "reset" }   # アプリ全体の背景透過

[mode]
normal_main = { fg = "#5ea1ff", bg = "reset" }   # モード表示も透過

[status]
percentage = { fg = "#ffffff", bg = "reset" }    # 「Top」「4K」等も透過
position   = { fg = "#ffffff", bg = "reset" }
```

### アイコン設定

```toml
[icon]
prepend_conds = [
    { if = "dir", text = "󰉋", fg = "#5ea1ff" }   # 塗りつぶしフォルダアイコン
]
prepend_dirs = [
    { name = ".config", text = "󰉋", fg = "#ff9800" },
    { name = ".git",    text = "󰉋", fg = "#00bcd4" }
]
```

> [!NOTE]
> アイコンの表示には **Nerd Fonts** 対応フォント（本環境では HackGen NF）が必要です．

### ステータスバーのセパレータ

```toml
[status]
sep_left  = { open = "", close = "" }   # 左セパレータ（なし）
sep_right = { open = "", close = "" }   # 右セパレータ（山括弧 < 形状）
```

---

## 🔗 外部ツール連携

| ツール | 役割 |
|--------|------|
| `bat` | テキストファイルのシンタックスハイライトプレビュー |
| `fd` | グローバルファイル検索（`F`キー） |
| `fzf` | ファジーファインダー連携 |
| `wsl-open` / `xdg-open` | ファイルを規定アプリで開く |
| `cyberdream.tmTheme` | batのシンタックステーマ（nvimと統一） |

---

## 💡 実践的なワークフロー例

### ファイルを素早くコピーして別ディレクトリに貼り付ける

```
1. コピーしたいファイルにカーソルを合わせる
2. yy でコピー（ヤンク）
3. l でディレクトリを移動（または h で上に戻る）
4. 目的のディレクトリで p でペースト
```

### 複数ファイルをまとめて削除する

```
1. v でビジュアル選択モードに入る
2. j / k で選択範囲を広げる（または Space で個別トグル）
3. d でゴミ箱へ（D で完全削除）
```

### シェルと連携してそのまま作業を続ける

```bash
# yazi を起動し，目的のディレクトリへ移動
yazi

# Yazi 内でディレクトリを移動してから q で終了
# → シェルが自動的に最後に開いていたディレクトリに移動している
pwd   # 確認
```

### プロジェクトのルートを素早く開く

```bash
# ghq 管理下のリポジトリをホームから直接 Yazi で開く
yazi ~/ghq/github.com/Naruto-Takahashi/
```

---

## 📚 参考リンク

- [Yazi 公式ドキュメント](https://yazi-rs.github.io/)
- [Yazi GitHub リポジトリ](https://github.com/sxyazi/yazi)
- [Cyberdream テーマ](https://github.com/scottmckendry/cyberdream.nvim)
- [Nerd Fonts](https://www.nerdfonts.com/)
